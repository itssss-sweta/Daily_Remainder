import 'package:flutter/material.dart';

const colpadding = EdgeInsets.fromLTRB(20, 10, 20, 10);

const buttonPadding = EdgeInsets.fromLTRB(20, 15, 20, 15);

const textPadding = EdgeInsets.fromLTRB(20, 10, 20, 10);

const contentPadding = EdgeInsets.fromLTRB(30, 20, 20, 20);

const String loginKey = "LOGIN";

const String registerKey = "REGISTER";

final GlobalKey<NavigatorState> navigationKey = GlobalKey<NavigatorState>();

const String taskKey = "TASK";

const String matchKey = "MATCH";
